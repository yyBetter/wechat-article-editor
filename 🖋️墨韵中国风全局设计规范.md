# 🖋️ 墨韵中国风全局设计规范

## 📖 设计理念

**墨韵中国风** - 融合传统中国文化与现代 AI 科技，打造独具东方美学的智能写作工具。

**核心特征**：
- 🎨 宣纸质感背景
- 🖋️ 水墨渐变元素
- 🔖 印章样式点缀
- 📜 传统书法字体
- ⚖️ 现代布局与传统美学的平衡

---

## 🎨 配色系统

### 主色调

```css
/* 墨色系 */
--ink-black: #2c3e50;        /* 浓墨黑 - 主标题、正文 */
--ink-grey: #5a6c7d;         /* 淡墨灰 - 次要文字 */

/* 朱砂系 */
--cinnabar-red: #d32f2f;     /* 朱砂红 - 强调、印章 */
--cinnabar-light: #ef5350;   /* 浅朱红 - 辅助强调 */

/* 宣纸系 */
--xuan-paper: #f9f6f0;       /* 宣纸米黄 - 主背景 */
--xuan-paper-dark: #f0ebe3;  /* 深宣纸 - 次背景 */

/* 金色系 */
--gold-accent: #d4af37;      /* 金色 - 装饰、边框 */
--gold-light: #e6c975;       /* 浅金 - 辅助装饰 */

/* 其他 */
--bamboo-green: #5f7c3e;     /* 竹绿 - 辅助色 */
--stone-grey: #95a5a6;       /* 石灰 - 分隔线 */
```

### 渐变系统

```css
/* 水墨渐变 */
--ink-gradient: linear-gradient(135deg, 
  rgba(44, 62, 80, 0.95) 0%, 
  rgba(90, 108, 125, 0.9) 100%
);

/* 朱砂渐变 */
--cinnabar-gradient: linear-gradient(135deg, 
  #d32f2f 0%, 
  #ef5350 100%
);

/* 金色渐变 */
--gold-gradient: linear-gradient(135deg, 
  #d4af37 0%, 
  #e6c975 100%
);

/* 宣纸渐变 */
--paper-gradient: linear-gradient(180deg, 
  #f9f6f0 0%, 
  #f0ebe3 100%
);
```

### 配色使用规则

#### ✅ 推荐组合
- **墨黑 + 金色** - 主标题、重要按钮
- **朱砂 + 宣纸** - 强调元素、徽章
- **墨灰 + 金色边框** - 次要元素、卡片

#### ❌ 避免组合
- 朱砂红 + 竹绿（对比过强）
- 多种渐变混用
- 纯黑配色（失去中国风韵味）

---

## 📐 字体系统

### 字体家族

```css
/* 基础字体 - 用于正文 */
--font-family-base: -apple-system, BlinkMacSystemFont, 
  'Segoe UI', 'PingFang SC', 'Hiragino Sans GB', 
  'Microsoft YaHei', sans-serif;

/* 标题字体 - 用于大标题 */
--font-family-title: 'Source Han Serif CN', 
  'Noto Serif SC', 'KaiTi', serif, 
  var(--font-family-base);
```

### 使用规则

| 元素 | 字体 | 字重 | 字间距 |
|------|------|------|--------|
| **Hero 主标题** | 标题字体 | 800 | 0.02em |
| **区块标题** | 标题字体 | 700 | 0.01em |
| **正文内容** | 基础字体 | 400 | 正常 |
| **按钮文字** | 基础字体 | 600-700 | 1-2px |
| **徽章标签** | 基础字体 | 700 | 0.5-1px |

---

## 🎭 核心组件样式

### 1. 宣纸纹理（全局可复用）

```css
.xuan-paper-texture {
  position: relative;
  background: var(--xuan-paper);
}

.xuan-paper-texture::before {
  content: '';
  position: absolute;
  inset: 0;
  background-image: 
    /* 横向纤维 */
    repeating-linear-gradient(
      90deg,
      transparent,
      transparent 2px,
      rgba(44, 62, 80, 0.015) 2px,
      rgba(44, 62, 80, 0.015) 4px
    ),
    /* 纵向纤维 */
    repeating-linear-gradient(
      0deg,
      transparent,
      transparent 2px,
      rgba(44, 62, 80, 0.015) 2px,
      rgba(44, 62, 80, 0.015) 4px
    ),
    /* 纸张瑕疵 */
    radial-gradient(circle at 20% 30%, 
      rgba(44, 62, 80, 0.02) 1px, transparent 1px),
    radial-gradient(circle at 60% 70%, 
      rgba(44, 62, 80, 0.02) 1px, transparent 1px),
    radial-gradient(circle at 90% 40%, 
      rgba(44, 62, 80, 0.02) 1px, transparent 1px);
  pointer-events: none;
  opacity: 0.6;
  z-index: 0;
}

.xuan-paper-texture > * {
  position: relative;
  z-index: 1;
}
```

**使用场景**：
- ✅ 大面积背景区域
- ✅ 内容丰富的区块
- ❌ 小卡片内部（纹理不明显）

### 2. 水墨边缘渐变

```css
.ink-edge-bottom {
  position: relative;
}

.ink-edge-bottom::after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  height: 120px;
  background: linear-gradient(
    to bottom,
    transparent 0%,
    rgba(44, 62, 80, 0.02) 50%,
    rgba(44, 62, 80, 0.05) 100%
  );
  pointer-events: none;
}
```

**使用场景**：
- ✅ 区块底部（模拟水墨晕染）
- ✅ 长内容区域的视觉过渡
- ✅ Hero Section 底部

### 3. 印章徽章样式

```css
.seal-badge {
  display: inline-flex;
  align-items: center;
  gap: 4px;
  padding: 6px 16px;
  border: 2px solid var(--cinnabar-red);
  color: var(--cinnabar-red);
  font-weight: 700;
  font-size: 13px;
  background: rgba(211, 47, 47, 0.05);
  transform: rotate(-2deg);
  box-shadow: inset 0 0 20px rgba(211, 47, 47, 0.08);
  border-radius: 2px;
  letter-spacing: 0.5px;
}

.seal-badge::before {
  content: '◆';
  font-size: 10px;
}
```

**使用场景**：
- ✅ "独家功能"、"核心成员"等标签
- ✅ 特殊身份标识
- ✅ 强调重要信息
- ❌ 不要过度使用（失去特殊感）

### 4. 竹简分隔线

```css
.bamboo-divider {
  height: 1px;
  background: linear-gradient(
    to right,
    transparent 0%,
    var(--gold-accent) 50%,
    transparent 100%
  );
  position: relative;
  margin: 40px 0;
}

.bamboo-divider::before {
  content: '◆';
  position: absolute;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -50%);
  color: var(--gold-accent);
  font-size: 10px;
  background: var(--xuan-paper);
  padding: 0 8px;
}
```

**使用场景**：
- ✅ 大区块之间的分隔
- ✅ 文章段落之间的装饰
- ✅ 内容层次的区分

---

## 🎨 阴影系统

### 阴影等级

```css
/* 轻微阴影 - 微交互 */
--shadow-sm: 0 2px 4px rgba(44, 62, 80, 0.08);

/* 中等阴影 - 卡片、按钮 */
--shadow-md: 0 4px 12px rgba(44, 62, 80, 0.12);

/* 明显阴影 - 悬停状态 */
--shadow-lg: 0 8px 24px rgba(44, 62, 80, 0.16);

/* 强阴影 - 模态框、弹出层 */
--shadow-xl: 0 16px 48px rgba(44, 62, 80, 0.20);

/* 水墨阴影 - 特殊强调 */
--shadow-ink: 0 4px 16px rgba(44, 62, 80, 0.25);
```

### 使用规则

| 元素 | 默认 | 悬停 | 激活 |
|------|------|------|------|
| **卡片** | sm | md | lg |
| **按钮** | md | ink | lg |
| **模态框** | xl | xl | xl |
| **下拉菜单** | lg | lg | lg |

---

## 📏 圆角系统

```css
--radius-sm: 4px;   /* 小元素：标签、徽章 */
--radius-md: 8px;   /* 中等：按钮、输入框 */
--radius-lg: 12px;  /* 大元素：卡片 */
--radius-xl: 16px;  /* 特大：模态框 */
```

### 使用原则

- ✅ 中国风倾向较小圆角（4-12px）
- ✅ 印章样式元素用 2-4px
- ❌ 避免超大圆角（失去方正感）
- ❌ 不使用 100px 胶囊按钮

---

## 🎬 动画系统

### 过渡动画

```css
/* 标准过渡 */
--transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);

/* 水墨过渡（更柔和）*/
--transition-ink: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
```

### 入场动画

```css
@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(30px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* 使用示例 */
.element {
  animation: fadeInUp 0.6s ease-out;
}
```

### 交互动画

```css
/* 悬停上浮 */
.card:hover {
  transform: translateY(-8px);
  transition: var(--transition-ink);
}

/* 按钮点击反馈 */
.button:active {
  transform: translateY(1px) scale(0.98);
}
```

### 装饰动画

```css
/* 脉冲闪烁 - 用于徽章圆点 */
@keyframes pulse {
  0%, 100% {
    opacity: 1;
    transform: scale(1);
  }
  50% {
    opacity: 0.6;
    transform: scale(1.1);
  }
}

/* 浮动 - 用于背景装饰 */
@keyframes float {
  0%, 100% {
    transform: translate(0, 0) scale(1);
  }
  33% {
    transform: translate(30px, -30px) scale(1.1);
  }
  66% {
    transform: translate(-30px, 30px) scale(0.9);
  }
}
```

---

## 🎯 组件设计规范

### 按钮

#### 主要按钮
```css
background: var(--ink-gradient);
color: white;
border: 2px solid var(--ink-black);
padding: 16px 40px;
border-radius: var(--radius-md);
box-shadow: var(--shadow-ink);
letter-spacing: 1px;
```

#### 次要按钮
```css
background: var(--xuan-paper);
color: var(--ink-black);
border: 2px solid var(--gold-accent);
padding: 16px 40px;
border-radius: var(--radius-md);
box-shadow: var(--shadow-sm);
letter-spacing: 1px;
```

#### 危险按钮
```css
background: var(--cinnabar-gradient);
color: white;
border: 2px solid var(--cinnabar-red);
```

### 卡片

```css
background: white;
border: 2px solid var(--color-border);
border-radius: var(--radius-lg);
box-shadow: var(--shadow-sm);
padding: 32px;
transition: var(--transition-ink);
```

```css
/* 悬停状态 */
.card:hover {
  transform: translateY(-8px);
  box-shadow: var(--shadow-lg);
  border-color: var(--gold-accent);
}
```

### 输入框

```css
background: var(--xuan-paper);
border: 2px solid var(--color-border);
border-radius: var(--radius-md);
padding: 12px 16px;
color: var(--ink-black);
```

```css
/* 聚焦状态 */
input:focus {
  border-color: var(--gold-accent);
  outline: none;
  box-shadow: 0 0 0 3px rgba(212, 175, 55, 0.1);
}
```

---

## 📱 响应式设计

### 断点系统

```css
/* 桌面端 */
@media (min-width: 1024px) {
  /* 标准布局 */
}

/* 平板端 */
@media (max-width: 768px) {
  /* 简化布局 */
  .nav-brand { font-size: 18px; }
  .hero-title { font-size: 40px; }
}

/* 手机端 */
@media (max-width: 480px) {
  /* 单列布局 */
  .hero-title { font-size: 32px; }
  .brand-name { display: none; }
}
```

### 响应式原则

- ✅ 移动端优先考虑可读性
- ✅ 保持宣纸纹理在所有设备上可见
- ✅ 按钮和交互区域至少 44x44px
- ✅ 文字大小不小于 14px

---

## 🎨 应用示例

### Hero Section

```css
.hero-section-v2 {
  background: var(--xuan-paper);
  padding: 120px 32px 80px;
  position: relative;
}

/* 宣纸纹理 */
.hero-section-v2::before {
  /* 纹理代码 */
}

.hero-title {
  font-family: var(--font-family-title);
  color: var(--ink-black);
  font-size: 64px;
  letter-spacing: 0.02em;
}

.gradient-text {
  background: var(--ink-gradient);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}

.hero-badge {
  border: 2px solid var(--cinnabar-red);
  color: var(--cinnabar-red);
  background: rgba(211, 47, 47, 0.08);
}
```

### 功能卡片

```css
.feature-card {
  background: white;
  border: 2px solid var(--color-border);
  border-radius: var(--radius-lg);
  padding: 40px;
}

.feature-icon {
  background: var(--ink-gradient);
  border: 2px solid var(--gold-accent);
  box-shadow: var(--shadow-ink);
}

.card-badge {
  background: var(--cinnabar-gradient);
  border: 2px solid var(--cinnabar-red);
}
```

---

## ✅ 设计检查清单

### 新组件设计时检查

- [ ] 是否使用了宣纸配色（#f9f6f0）
- [ ] 是否应用了合适的纹理效果
- [ ] 标题是否使用了衬线字体
- [ ] 是否使用了中国风渐变
- [ ] 圆角是否适中（4-12px）
- [ ] 是否添加了柔和的阴影
- [ ] 动画是否使用了水墨过渡
- [ ] 是否有印章或金色装饰点缀
- [ ] 移动端是否可读
- [ ] 是否保持了整体风格一致性

---

## 🚫 常见错误

### ❌ 不要做

1. **过度使用朱砂红**
   - 朱砂红是强调色，不要大面积使用
   - 应保留给重要的 CTA 和徽章

2. **忽略宣纸纹理**
   - 纹理是中国风的核心
   - 每个主要区块都应有纹理

3. **使用圆形胶囊按钮**
   - border-radius: 100px 不符合中国风
   - 应使用 4-12px 的小圆角

4. **纯黑纯白配色**
   - 避免使用 #000 和 #fff
   - 应使用墨黑和宣纸色

5. **过于复杂的装饰**
   - 少即是多
   - 装饰应当克制、有节制

### ✅ 应该做

1. **保持平衡**
   - 传统元素与现代布局并存
   - 不要过度传统化

2. **注重细节**
   - 字间距、行高要舒适
   - 阴影要柔和自然

3. **统一语言**
   - 所有页面保持一致的视觉风格
   - 复用设计组件

---

## 📚 设计资源

### 推荐字体
- **衬线**: Source Han Serif CN（思源宋体）
- **衬线**: Noto Serif SC（思黑体）
- **衬线**: KaiTi（楷体）- 系统自带
- **无衬线**: PingFang SC（苹方）
- **无衬线**: Microsoft YaHei（微软雅黑）

### 图标风格
- 线性图标优先
- 避免过于扁平
- 可适当使用水墨风格图标

### 图片处理
- 饱和度降低 10-20%
- 添加宣纸纹理叠加
- 边缘可添加水墨晕染效果

---

## 🎉 总结

墨韵中国风设计系统的核心是：
- 🖼️ **宣纸质感** - 温润的背景
- 🖋️ **水墨渐变** - 流动的美感  
- 🔖 **印章点缀** - 传统的韵味
- 📜 **书法字体** - 文化的传承
- ⚖️ **现代布局** - 科技的力量

通过这些元素的组合，我们打造了一个**既有文化底蕴，又不失现代感**的 AI 写作工具界面。

---

*墨韵中国风 - 让每一次创作都充满东方美学* 🎨✨

