# 🐛 快速修复：platformId未定义错误

## 问题
```
Uncaught ReferenceError: platformId is not defined
    at MultiPlatformAdapter.tsx:450:41
```

## 原因
在Tab切换的代码重构中，使用了错误的变量名：
- ❌ 使用了 `platformId`（未定义）
- ✅ 应该使用 `activePlatformTab`（当前选中的平台）

## 修复位置
1. **第439行**：复制按钮的点击事件
   ```typescript
   onClick={() => copyToClipboard(activePlatformTab)}
   ```

2. **第450行**：平台预览组件的传参
   ```typescript
   platform={activePlatformTab as any}
   ```

---

**修复完成！刷新浏览器测试** ✅
